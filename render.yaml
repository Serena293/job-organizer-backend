services:
  - type: web
    name: job-organizer
    runtime: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/job-organizer-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17